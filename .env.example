# ===================================================================
# Configuration d'environnement pour le projet ETAC
# ===================================================================
# INSTRUCTIONS:
# 1. Copiez ce fichier en .env : cp .env.example .env
# 2. Modifiez les valeurs dans .env selon votre environnement
# 3. NE COMMITEZ JAMAIS le fichier .env (il contient des secrets)
# ===================================================================

# === Django Core Settings ===
# IMPORTANT: En production, générez une clé aléatoire sécurisée de 50+ caractères
# Outil: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=change-me-in-production-generate-random-string-50-chars-minimum

# Mode debug (True en développement, False en production)
DEBUG=True

# Hosts autorisés (séparés par des virgules, sans espaces)
# Exemple production: ALLOWED_HOSTS=example.com,www.example.com
ALLOWED_HOSTS=localhost,127.0.0.1

# === API INSEE ===
# Obtenez votre clé API sur: https://api.insee.fr/catalogue/
# Nécessaire pour la validation des numéros SIREN
INSEE_API_KEY=your-insee-api-key-get-it-from-api.insee.fr

# === Base de données (Production uniquement) ===
# Par défaut, SQLite est utilisé en développement
# Décommentez et configurez ces variables pour utiliser MySQL/PostgreSQL en production
# DB_NAME=db_name
# DB_USER=your_db_user
# DB_PASSWORD=your_db_password
# DB_HOST=localhost
# DB_PORT=3306
